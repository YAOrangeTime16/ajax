{
  "name": "game-app",
  "version": "1.0.0",
  "description": "api assignment",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gulp-browserify": "^0.5.1",
    "pokersolver": "^2.1.2",
    "uniq": "^1.0.1"
  }
}

browserify ./js/script_poker.js -o bundle.js